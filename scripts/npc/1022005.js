var city = '勇士部落';
var name = '王先生';
var code = '1022005';

/**
 * 仓库管理
 * @param mode
 * @param type
 * @param selection
 */
function action(mode, type, selection) {
    cm.sendStorage();
    cm.dispose();
}